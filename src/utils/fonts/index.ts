import { Inter, Roboto_Mono, Sen, Solway, Outfit } from "next/font/google"

export const inter = Inter({
    subsets: ["latin"],
    display: "swap",
})

export const roboto_mono = Roboto_Mono({
    subsets: ["latin"],
    display: "swap",
})

export const sen = Sen({
    subsets: ["latin"],
    display: "swap",
})

export const solway = Solway({
    subsets: ["latin"],
    display: "swap",
    weight: ["400", "700"],
})

export const outfit = Outfit({
    subsets: ["latin"],
    display: "swap",
})
